package pl.sda.patterns.creational.factory.abstractfactory;

public class CarFactory implements AbstractFactory<CarItem>{
    @Override
    public CarItem create(CarItem item) {
        return null;
    }
}
