package pl.sda.patterns.creational.factory.abstractfactory;

public class BikeFactory implements AbstractFactory<BikeItem> {
    @Override
    public BikeItem create(BikeItem item) {
        return null;
    }
}
